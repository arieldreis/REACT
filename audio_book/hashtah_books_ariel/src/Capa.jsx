function Capa(props){
    return <img className="capa" src={props.imagemCapa} alt={props.textoAlternativo} />
}

export default Capa;
// O attibuto className é mesma coisa que o class do html é diferença é que o className só usado no REACT.